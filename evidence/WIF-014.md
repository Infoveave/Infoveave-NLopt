# WIF-014 — Clean published consumption evidence

## Status

Blocked pending the first remote stable publication. Local clean-consumer fixtures and negative-path tests are implemented, but local project/package artifacts are not substitutes for the published GitHub Packages and GitHub Release assets required by WIF-014.

## Evidence still required

- Stable tag, tested commit, package version, feed URL, release URL, and immutable asset checksums.
- Fresh-cache restore from the real package feed on `linux-x64`, `win-x64`, and `osx-arm64`.
- Explicit runtime acquisition followed by offline build/run and publish/run on every supported platform.
- Constrained solving, cancellation, repeated disposal, absent/wrong/corrupt bundle, transitive use, and class-library-only consumption from published output.
- Authorized stable publication and repetition against that exact released pair.

Do not mark this task Done or start the dependent What-If implementation until those hosted results are recorded here.
